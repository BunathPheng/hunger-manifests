---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hunger                  # Ingress name
  labels:
    app: hunger
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "hunger.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hunger               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
